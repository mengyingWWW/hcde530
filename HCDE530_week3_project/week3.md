# Week 3 — Competency Claim

## Competency Claim Rubric — HCDE 530 SP26

| Criteria | Demonstrates clearly | Partially demonstrates | Not addressed |
| --- | --- | --- | --- |
| Competency claim(s) | | | |

## Competency claim(s)

This week, I debugged `week3_analysis_buggy.py` by reproducing the crash, reading the traceback, and fixing a `ValueError` caused by messy CSV data (`"fifteen"` in `experience_years`). I also fixed the ranking logic so the script correctly returns the highest five satisfaction scores instead of the lowest values.

I demonstrated data-cleaning competency by normalizing role values, handling inconsistent inputs safely, and writing a cleaned output file (`week3_survey_cleaned.csv`). I added plain-English inline comments to document the key cleaning steps and committed each major fix with specific messages so the debugging process is visible in repo history.
